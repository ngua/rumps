; Test Fallible:flat-map rejects Result/Option mismatch

fun main() {
  let res = Result.Ok(42) : Result[Int, String]
  let bad = Fallible:flat-map((x: Int) => Option.Some(x + 1), res) : Option[Int]
  write bad
}
