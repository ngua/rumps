[package]
name = "rumps-storage"
version = "0.0.1"
edition = "2021"

[dependencies]
rumps-types = { path = "../rumps-types" }
async-trait = "0.1"
bincode = "1.3"
bitvec = "1.0"
crc32fast = "1.4"
futures = "0.3"
lru = "0.12"
serde = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }

# Benchmark dependencies (only when bench feature is enabled)
criterion = { workspace = true, optional = true }
tempfile = { version = "3.14", optional = true }

[dev-dependencies]
criterion = { workspace = true }
serde_json = "1.0"
tempfile = "3.14"
tokio-test = "0.4"

[features]
bench = ["criterion", "tempfile"]

[[bench]]
name = "btree_bench"
harness = false
required-features = ["bench"]

[[bench]]
name = "wal_bench"
harness = false
required-features = ["bench"]

[lints.rust]
dead_code = "allow"

[lints.clippy]
obfuscated_if_else = "allow"
type_complexity = "allow"
use_self = "warn"
unwrap_used = "deny"
expect_used = "deny"
