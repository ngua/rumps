; Same as 221 but with `foo` defined AFTER `main`, exercising
; Phase 4 retroactive constraint propagation through the
; hoisted Pass 1 scheme.

fun main() {
  write foo("hello")
}

fun foo(x) { x + 1 }
