; Iterable constraint rejects Range for String (Range is Iterable over Int only)

fun main() {
  ; This should fail: Range is Iterable over Int, not String
  write process-strings(1 .. 10)
}

fun process-strings[I: Iterable](i: I[String]) -> Int {
  Iterable:length(i)
}
