; Mutual recursion where `b` has a body-derived `Additive`
; constraint. Forward reference from `a` to `b` must propagate
; the constraint via Phase 4 finalization. Calling `a("hello")`
; should be rejected.

fun a(x) {
  b(x)
}

fun b(x) {
  x + 1
}

fun main() {
  write a("hello")
}
