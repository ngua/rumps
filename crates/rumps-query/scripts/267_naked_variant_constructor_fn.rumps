; Naked payload variant constructors are first-class functions.

#(deriving: Display)
variant Box[T] = Full(T) | Empty

fun main() {
  let some = .Some
  let full = .Full

  write :display(some(42))
  write :display(full("ok"))
}
