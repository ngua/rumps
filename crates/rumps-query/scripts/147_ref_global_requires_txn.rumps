; Error test: global Ref variable write outside transaction

FUN main() {
  ; Create a global ref
  LET g = ^info{"test-key"}

  ; This should fail: global write requires transaction
  @SET g "should fail"
}
