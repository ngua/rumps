; Error: passing object missing required fields to function

NEWTYPE Person = { name: String, age: Int }

FUN greet (p: Person) -> String {
  "Hello, {p.name}!"
}

; Missing `age` field
WRITE greet({ name: "Alice" })
