; Test: Global writes outside transaction are rejected at compile time
; This test should fail during typechecking, not at runtime.

FUN main() {
  ; Direct global SET outside transaction
  @SET ^data{1} "value"

  WRITE "should not reach here"
}
