fun main() {
  write :display(P.f(P.mk(1 .. 10)))
}

module P {
  +newtype A[T] = Array[T]

  +fun mk[T](xs: Array[T]) -> A[T] {
    xs as A[T]
  }

  +fun f[T](x: A[T]) -> Option[T] {
    let y = x as Array[T]
    :get(y, 0)
  }
}
