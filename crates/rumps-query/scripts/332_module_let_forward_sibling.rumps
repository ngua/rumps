; Module `let` consumers can appear before provider modules.

module UseImport {
  import SourceImport.{ v }

  +let w = () => v + 1
}

module UsePath {
  +let w = () => SourcePath.v + 1
}

module SourceImport {
  +let v = 41
}

module SourcePath {
  +let v = 41
}

fun main() {
  write 0
}
