; `b` has an undeclared `Additive` on explicit `T`. The constraint
; does NOT propagate to `a` (since `b`'s scheme won't include it),
; so only `b` errors.

fun a[T](x: T) -> T {
  b(x)
}

fun b[T](x: T) -> T {
  x + 1
}

fun main() {
  write a(1)
}
