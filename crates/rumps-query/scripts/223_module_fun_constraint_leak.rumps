; Calling `M.foo("hello")` must be rejected because `T` is
; constrained to `Numeric`. Module member constraint check.

module M {
  +fun foo[T: Numeric](x: T) -> T { x + 1 }
}

fun main() {
  write M.foo("hello")
}
