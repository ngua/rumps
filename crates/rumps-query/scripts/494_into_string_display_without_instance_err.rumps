module HiddenLabel {
  #(deriving: Display)
  +newtype Label = Int

  +fun make(n: Int) -> HiddenLabel.Label {
    n as HiddenLabel.Label
  }
}

fun main() {
  let x = HiddenLabel.make(7)
  write x as String
}
