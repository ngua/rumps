; Module mutual recursion: `M.a` calls `M.b` which has a body-derived
; `Additive` constraint. The enriched scheme must also update the
; module member registry so `M.a("hello")` is rejected.

module M {
  +fun a(x) {
    b(x)
  }

  +fun b(x) {
    x + 1
  }
}

fun main() {
  write M.a("hello")
}
