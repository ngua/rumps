; Module `let` shadowing is allowed.

module M {
  +fun len() -> Int { 1 }

  +let len = Iterable:length
  +let len = x => x + 1

  +let value = 10
  +let value = value + 32
}

fun main() {
  write M.len(41)
  write M.value
}
