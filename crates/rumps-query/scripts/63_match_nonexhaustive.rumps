; Non-exhaustive match error

FUN main() {
  LET x = 5

  ; This should fail because no arm matches
  LET result = MATCH x {
    1 => { "one" }
    2 => { "two" }
    3 => { "three" }
  }

  WRITE result
}
