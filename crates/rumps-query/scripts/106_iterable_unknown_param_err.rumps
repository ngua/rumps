; Error: unknown type parameter in Iterable[X] constraint

FUN main() {
  ; Closure case
  LET bad-closure = [T: Iterable[Y]](coll: T) -> Int => 0
}

; Function case
FUN bad[T: Iterable[X]](coll: T) -> Int {
  0
}
