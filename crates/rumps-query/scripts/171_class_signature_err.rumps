; Error: wrong method signature
;
; The Display class requires `display(self) -> String` but we return Int.

FUN main() { }

TYPE Point = Point(Int, Int)

CLASS Display FOR Point {
  FUN display(p: Point) -> Int {
    42
  }
}
