; Unknown variant in pattern error

LET opt = Option.Some(42)

; This should fail because Option.Unknown doesn't exist
LET result = MATCH opt {
  Option.Unknown(v) => { v }
  Option.None => { 0 }
}

$OUTPUT result
