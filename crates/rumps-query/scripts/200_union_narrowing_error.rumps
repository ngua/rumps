; Union narrowing via type annotation should be rejected.
; Use MATCH/IS for runtime type narrowing instead.

fun main() {
  let r = 1 ..= 10
  ; `Reversible:reverse` on `Range` returns `Range`
  let revd: Array[Char] = Reversible:reverse(r)
  write revd
}
