; Pattern arity mismatch error

fun main() {
  let opt = Option.Some(42)

  ; This should fail because Option.Some has 1 payload, not 2
  let result = match opt {
    Option.Some(a, b) => { a + b }
    Option.None => { 0 }
  }

  write result
}
