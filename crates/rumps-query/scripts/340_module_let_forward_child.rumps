; Module `let`s can reference later child module `let` members.

module M {
  import M.Child.{ v }

  +let x = () => M.Child.v + 1
  +let y = () => v + 1

  module Child {
    +let v = 41
  }
}

fun main() {
  write 0
}
