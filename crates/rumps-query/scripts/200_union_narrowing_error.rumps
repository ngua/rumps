; Union narrowing via type annotation should be rejected.
; Use MATCH/IS for runtime type narrowing instead.

fun main() {
  let r = 1 ..= 10
  ; reverse returns Array[Int] | Range; cannot narrow to Array[Int]
  let revd: Array[Int] = reverse(r)
  write revd
}
