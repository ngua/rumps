; Error: HKT class Iterable does not accept type args in constraint position

fun main() {
  ; Closure case
  let bad-closure = [T: Iterable[Y]](coll: T) -> Int => 0
}

; Function case
fun bad[T: Iterable[X]](coll: T) -> Int {
  0
}
